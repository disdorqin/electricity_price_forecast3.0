# Core scientific / ML stack
numpy>=1.23
pandas>=1.5
scikit-learn>=1.2
scipy>=1.10
xgboost>=2.0
catboost>=1.2
lightgbm>=4.0

# Deep learning (RT916_SpikeFusionNet / TimeMixer use torch)
torch>=2.0

# Visualization
matplotlib>=3.5

# I/O & utilities
pyyaml>=6.0
joblib>=1.2
python-dotenv>=1.0
pymysql>=1.1
openpyxl>=3.0           # required by pandas.read_excel
chinese-calendar>=1.10
borax>=1.4

# Variational Mode Decomposition (used by SGDFNet/RT916 fusion runner)
vmdpy>=0.2

# TimesFM dependencies
huggingface_hub[cli]>=0.23.0
safetensors>=0.5.3
# timesfm[xreg] is installed locally via: pip install -e <epf_project>/TF[xreg]
timesfm>=1.0            # or local editable install from epf/TF

# TimesFM uses the Flax/JAX stack
einshape>=1.0
flax>=0.8
jax>=0.4
jaxtyping>=0.2
orbax-checkpoint>=0.4
