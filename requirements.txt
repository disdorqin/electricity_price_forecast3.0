# ============================================================
# 山东电力现货市场预测系统 — Python 依赖
# ============================================================
# 安装: pip install -r requirements.txt
# 环境: Python >= 3.10, 推荐 conda 环境
# ============================================================

# ── Core scientific / ML stack ──
numpy>=1.23
pandas>=1.5
scikit-learn>=1.2
scipy>=1.10
xgboost>=2.0
catboost>=1.2
lightgbm>=4.0

# ── Deep learning ──
# RT916_SpikeFusionNet / TimeMixer / TimesFM(PyTorch backend) 均依赖 torch
torch>=2.0

# ── I/O & utilities ──
pyarrow>=14.0             # parquet 读写
pyyaml>=6.0
joblib>=1.2
python-dotenv>=1.0
pymysql>=1.1              # sync_dataset 数据库同步
openpyxl>=3.0             # pandas.read_excel 需要
chinese-calendar>=1.10    # 节假日判断
borax>=1.4                # 农历日期
matplotlib>=3.5

# ── SGDFNet / RT916 融合 ──
vmdpy>=0.2                # Variational Mode Decomposition

# ── TimesFM 依赖 ──
# TimesFMBackend/ 是 bundled 本地模块，不需要 pip install。
# 预训练权重通过 huggingface_hub 下载。
huggingface_hub[cli]>=0.23.0   # 下载预训练权重
safetensors>=0.5.3             # 加载 .safetensors 权重文件
jax>=0.4                       # TimesFM xreg 协变量回归模块需要

# ── TimesFM Flax 后端（可选）──
# 以下依赖仅在使用 TimesFM Flax 后端时需要。本项目使用 PyTorch 后端，
# 一般不需要取消注释。
# einshape>=1.0
# flax>=0.8
# jaxtyping>=0.2
# orbax-checkpoint>=0.4
