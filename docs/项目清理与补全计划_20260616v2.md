# 项目清理与补全计划（2026-06-16 v2）

## 当前状态确认

✅ 统一入口基础设施存在：cli/、pipelines/、runners/、services/、utils/
✅ 融合链路可运行：2026-05-01 DA/RT/joint 产出完整
✅ 5 个模型目录存在
❌ 项目根目录仍然杂乱
❌ RT916 的 dataprocess/model 未提到根目录
❌ LightGBM/TimesFM 只有 pipeline.py 包装（用户确认不需要内部分拆）

## 第一部分：根目录清理

### 保留（核心结构）

```
electricity_forecast_model2.0/
├── main.py                          # 统一入口
├── README.md                        # 项目说明
├── requirements.txt                 # 依赖
├── .gitignore                       # Git 忽略规则
├── LICENSE                          # MIT 许可证
├── cli/                             # CLI 解析
├── pipelines/                       # 流水线编排
├── runners/                         # 模型调度
├── services/                        # 服务层
├── utils/                           # 共享工具
├── configs/                         # 统一配置（如已有）
├── data/                            # 主数据集
├── docs/                            # 项目文档
├── fusion/                          # 融合代码
├── fusion_runs/                     # 融合实验产物
├── models/                          # 训练好的模型权重
├── TimeMixer/                       # 模型 1
├── SGDFNet/                         # 模型 2
├── RT916_SpikeFusionNet/            # 模型 3
├── lightGBM/                        # 模型 4
├── TimesFM/                         # 模型 5
└── ExtremPriceClf/                  # 极端价格分类器
```

### 归档（移入 `_archive/`）

| 项目 | 理由 |
|------|------|
| `START_HERE.md` | 与 README.md 重复 |
| `MEMORY.md` | 移入 docs/ |
| `run_monthly_repro_suite.py` | 移入 fusion/runners/ 或归档 |
| `outputs/` | 根目录级别的实验产物堆（RT916/SGDFNet 的旧输出），移入 `_archive/outputs_root/` |
| `TF/` | 旧版 TimesFM 代码（price_forecast_copy_分时段预测.py），已被 TimesFM/ 取代 |
| `scripts/` | 一次性工具脚本，移入 `_archive/scripts/` |
| `_archive/repro_cleanup_20260613/` | 已有归档，保留不动 |

### 各模型内部清理

**TimeMixer/**：
- 移入 `_archive/`：`outputs/`、`outputs_v2/`、`candidate_configs/`、`enhanced_*` 系列（enhanced_pipeline.py、enhanced_model.py、enhanced_config.py、enhanced_loss.py）
- 保留：`dataprocess.py`、`model.py`、`pipeline.py`、`backbones.py`、`repro_pipeline.py`、`pipeline_timemixer.py`、`__init__.py`

**SGDFNet/**：
- 移入 `_archive/`：42 个废弃 config（只留 cutoff_recovery 和 production 两个）、`outputs/`、`research_control/`、`docs/PACKAGING_CHANGELOG.md`
- 保留：`src/sgdfnet/`、`scripts/run_protocol_b_cutoff.py`、2 个核心 config、`README.md`、`dataprocess.py`、`model.py`、`pipeline.py`

**RT916_SpikeFusionNet/**（可重构）：
- 将 `src/rt916_spikefusionnet/dataprocess.py` 和 `src/rt916_spikefusionnet/model.py` 在根目录创建引用文件（re-export 或薄包装）
- 移入 `_archive/`：`FINAL_PACKAGING_SUMMARY.md`（已归档）、`README_RT916.md`（已归档）、`train.py`（硬编码日期的开发脚本）
- 保留：`pipeline.py`、`run.py`、`src/rt916_spikefusionnet/`、`configs/`、`README.md`、`docs/`

**lightGBM/**（不改内部结构）：
- 移入 `_archive/`：`outputs/`（旧预测输出）、`lightGBM_oneday.py`（standalone 开发脚本）
- 保留：`pipeline.py`、`main_fix.py`、`train_fix.py`、`train_da_fix.py`、`infer_fix.py`、`infer_da_fix.py`、`__init__.py`、`requirements.txt`

**TimesFM/**（不改内部结构）：
- 移入 `_archive/`：`output/`（旧预测输出）、`price_forecast_copy_分时段预测.py`（已被 pipeline.py 取代）
- 保留：`pipeline.py`、`infer.py`、`src/timesfm/`、`__init__.py`、`pyproject.toml`、`requirements.txt`、`README.md`

## 第二部分：RT916 补全

RT916 的 `dataprocess.py` 和 `model.py` 在 `src/rt916_spikefusionnet/` 内部。在根目录创建薄包装：

```python
# RT916_SpikeFusionNet/dataprocess.py
"""RT916 data processing - re-exports from internal package."""
from src.rt916_spikefusionnet.dataprocess import *
```

```python
# RT916_SpikeFusionNet/model.py
"""RT916 model definitions - re-exports from internal package."""
from src.rt916_spikefusionnet.model import *
from src.rt916_spikefusionnet.annual_model import *
from src.rt916_spikefusionnet.annual_model_da_timemixer import *
```

## 第三部分：LightGBM / TimesFM 接口确认

用户明确不改这两个模型的内部结构，只需要统一接口。当前状态：

**lightGBM/pipeline.py** ✅ 已存在（Codex 创建）
**TimesFM/pipeline.py** ✅ 已存在（Codex 创建）

只需验证这两个 pipeline.py 是否正确暴露了 `ModelPipeline` 接口（train/predict/predict_range）。

## 第四部分：验证

清理完成后，运行统一入口验证链路完整：

```bash
# 用 conda 环境 epf-2 的 Python 直接运行
D:\computer_download\environment\conda\epf-2\python.exe main.py --pipeline predict --target both --models all --date 2026-05-01
```

确认产出：
- DA 预测 CSV
- RT 预测 CSV
- 融合结果 CSV

## 执行顺序

1. 根目录清理（归档 6 项）
2. 各模型内部清理（归档 outputs/废弃文件）
3. RT916 根目录薄包装
4. 验证 LightGBM/TimesFM pipeline.py 接口
5. 端到端烟测（2026-05-01）
